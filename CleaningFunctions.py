import subprocess

class GetCleaningFunctions:
    def __init__(self):
        pass
    def sanitize_text(self, text):
        match = True
        if match:
            return text
        else:
            return text
    
    # Bare bones TBC
    # Not yet implemented.
    
